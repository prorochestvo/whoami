# build outputs, scratch and logs (only these three are gitignored at root)
/build/
/tmp/
/logs/

# secrets and local env
.env
.env.*

# os / editor cruft
.DS_Store
.idea/

# Claude Code: commit settings, skills, and agent memory
.claude/*
!.claude/settings.json
!.claude/skills/
!.claude/agent-memory/
